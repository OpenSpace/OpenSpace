#pragma once

namespace mol::rep {

enum class Type {
    SpaceFill,
    Licorice,
    Ribbons,
    Cartoon,
};

enum class Color {
    Cpk,
    AtomIndex,
    ResId,
    ResIndex,
    ChainId,
    ChainIndex,
    SecondaryStructure,
    Uniform
    // Property
};

}
