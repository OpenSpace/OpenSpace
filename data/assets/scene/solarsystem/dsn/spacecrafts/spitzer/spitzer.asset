local assetHelper = asset.require('util/asset_helper')
local dataFolder = openspace.absPath("${SYNC}/http/dsn_data/1/positioning/STF") 

local models = asset.syncedResource({
    Name = "Dsn models",
    Type = "HttpSynchronization",
    Identifier = "dsn_stations",
    Version = 1
})
local spacecraftID = "Spitzer";

local Spitzer = {
    Identifier = spacecraftID,
    Transform = {
      Translation = {
          Type = "RadecTranslation", 
          DataFolder = dataFolder,
          DataFileType = "json",
          ObjectIdentifier = spacecraftID
      }

   },
    GUI = {
        Name = "Spitzer",
        Path = "/Solar System/Missions/Spitzer"
    }
}
asset.export("spacecraftID", spacecraftID)
assetHelper.registerSceneGraphNodesAndExport(asset, {Spitzer})