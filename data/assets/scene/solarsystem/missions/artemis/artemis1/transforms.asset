local earthAsset = asset.require("scene/solarsystem/planets/earth/transforms")
local kernels = asset.require("./kernels")
local coreKernels = asset.require("spice/core")



local ArtemisPosition = {
  Identifier = "ArtemisPosition",
  Parent = earthAsset.EarthCenter.Identifier,
  Transform = {
    Translation = {
      Type = "SpiceTranslation",
      Target = kernels.ID.Artemis,
      Observer = coreKernels.ID.Earth,
      Frame = coreKernels.Frame.Galactic
    }
  },
  GUI = {
    Name = "Artemis I Position",
    Path = "/Solar System/Missions/Artemis I",
    Focusable = false,
    Hidden = true
  }
}


asset.onInitialize(function()
  openspace.addSceneGraphNode(ArtemisPosition)
end)

asset.onDeinitialize(function()
  openspace.removeSceneGraphNode(ArtemisPosition)
end)

asset.export(ArtemisPosition)



asset.meta = {
  Name = "Artemis I Transforms",
  Description = "Artemis I transforms, position relative to Earth",
  Author = "OpenSpace Team",
  URL = "https://openspaceproject.com",
  License = "MIT license"
}
