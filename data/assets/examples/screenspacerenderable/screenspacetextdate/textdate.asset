-- Basic
-- This example creates a basic screen space renderable that shows the current simulation
-- time. See other examples for how to customize the output format of the date.

local Item = {
  Type = "ScreenSpaceTextDate",
  Identifier = "ScreenSpaceTextDate_Example",
  Scale = 1.75
}

asset.onInitialize(function()
  openspace.addScreenSpaceRenderable(Item)
end)

asset.onDeinitialize(function()
  openspace.removeScreenSpaceRenderable(Item)
end)
